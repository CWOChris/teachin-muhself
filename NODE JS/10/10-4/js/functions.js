









const function = {
    fullName: (firstName, lastName) => {
        return `${firstName} ${lastName}`;
    },
    addDoctor: (lastName) => {
        return `Dr. ${lastName}`;
    },
};

module.exports = function;