const firstName = 'Tanner';

console.log(`Welcome to class ${firstName}!`);

const car = {
    make: "Mazda",
    model: "Miata",
};

// console.log(firstName + " drives a " + car.make + " " + car.model + ".");

console.log(`${firstName} drives a ${car.make} ${car.model}.`);